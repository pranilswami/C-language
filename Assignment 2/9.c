#include<stdio.h>
#include<conio.h>

int main()
{
    int a;
    char b;
    float c;
    double d;
    printf("size of int variable %d",sizeof(a));
    printf("\nsize of int variable %c",sizeof(b));
    printf("\nsize of int variable %f",sizeof(c));
    printf("\nsize of int variable %lf",sizeof(d));
    return 0;
   
}